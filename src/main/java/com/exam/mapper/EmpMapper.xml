<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.exam.mapper.EmpMapper">
	
	<!-- 로그인 -->
	<select id="login" parameterType="hashmap" resultType="EmpDTO">
		select *
		from emp
		where emp_id=#{emp_id} and emp_pw=#{emp_pw}
	</select>
	
	<!-- id 중복체크 -->
	<select id="idCheck" parameterType="string" resultType="EmpDTO">
		select *
		from emp
		where emp_id=#{emp_id}
	</select>
	
	<!-- 회원가입 -->
	<insert id="empAdd" parameterType="EmpDTO">
		insert into emp(emp_id, admin_id, emp_pw, name, birth, phone, account, position, hourly_wage, week_time)
		values (#{emp_id}, #{admin_id}, #{emp_pw}, #{name}, #{birth}, #{phone}, #{account}, #{position}, #{hourly_wage}, #{week_time})
	</insert>
	
</mapper>

